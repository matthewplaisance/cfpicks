{
    "rules": {
      "users": {
        "$uid": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        }
      }
    },
        "rules1": {
          ".read": "auth != null", 
          ".write": "auth != null" 
        }
  }
  